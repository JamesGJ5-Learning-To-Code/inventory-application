extends layout

block content
  h1= title
  p Welcome to #{title}

  if error 
    p Error getting dynamic content 

  else
    P For your information, the catalog has the following number of categories and items:
    
    ul 
      li #[strong Category Count:] !{categoryCount}
      li #[strong Item Count:] !{itemCount}