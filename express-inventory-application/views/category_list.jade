extends layout 

block content 
    h1= title

    ul 
        each categoryDoc in categoryDocArray 
            li 
                a(href=`/catalog/category/${categoryDoc.url}`) #{categoryDoc.name}
